import { VirtualNode } from "./interfaces";

/**
 * Applies the diffed changes to the DOM
 * @param newVNode
 * @param oldVNode 
 * @param document 
 */
export default function render(newVNode: VirtualNode, oldVNode: VirtualNode, document: Document): void {

}